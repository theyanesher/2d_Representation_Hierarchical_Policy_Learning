defaults:
  - _self_
  - task: high_level_heatmap_cnn_zarr_dataloader_EARLY_RVT_Heatmap

name: RVT_Heatmap_ALL_Tasks_EARLY #DEBUG_ONE_EPISODE_INSERT_SQ_PEG #RESTART_diffusion_unet_hybrid_wrist_language_16_Tasks_EARLY # FINAL_diffusion_unet_hybrid_wrist_language_16_Tasks_LATE_Fusion #_DUMMY_TESTING_EARLY_FUSION #train_diffusion_unet_hybrid_multitask_wrist_language_Whole_dataset_16_Tasks_EARLY_Fusion #train_diffusion_unet_hybrid_multitask_wrist_language_Whole_dataset_16_Tasks_LATE_Fusion #_DUMMY_TESTING_LATE_FUSION #train_diffusion_unet_hybrid_multitask_wrist_language_Whole_dataset_16_Tasks_LATE_Fusion   # WHOLE_DATASET_WRIST_high_level_heatmap_cnn_zarr_dataloader
_target_: diffusion_policy.workspace.train_diffusion_unet_hybrid_workspace.TrainDiffusionUnetHybridWorkspace

task_name: ${task.name}
shape_meta: ${task.shape_meta}
exp_name: "default"

horizon: 16
n_obs_steps: 2
n_action_steps: 8
n_latency_steps: 0
dataset_obs_steps: ${n_obs_steps}
past_action_visible: False
keypoint_visible_rate: 1.0
obs_as_global_cond: True


policy:
  _target_: diffusion_policy.policy.diffusion_unet_hybrid_image_policy.DiffusionUnetHybridImagePolicy

  shape_meta: ${shape_meta}
  
  noise_scheduler:
    _target_: diffusers.schedulers.scheduling_ddpm.DDPMScheduler
    num_train_timesteps: 100
    beta_start: 0.0001
    beta_end: 0.02
    beta_schedule: squaredcos_cap_v2
    variance_type: fixed_small # Yilun's paper uses fixed_small_log instead, but easy to cause Nan
    clip_sample: True # required when predict_epsilon=False
    prediction_type: epsilon # or sample

  seperate_open_close: False
  horizon: ${horizon}
  n_action_steps: ${eval:'${n_action_steps}+${n_latency_steps}'}
  n_obs_steps: ${n_obs_steps}
  num_inference_steps: 100
  obs_as_global_cond: ${obs_as_global_cond}
  crop_shape: [76, 76]
  # crop_shape: null
  diffusion_step_embed_dim: 128
  down_dims: [512, 1024, 2048]
  kernel_size: 5
  n_groups: 8
  cond_predict_scale: True
  obs_encoder_group_norm: True
  eval_fixed_crop: True



# policy:
#   _target_: diffusion_policy.policy.diffusion_unet_hybrid_image_policy.DiffusionUnetHybridImagePolicy

#   shape_meta: ${shape_meta}
  
#   noise_scheduler:
#     _target_: diffusers.schedulers.scheduling_ddpm.DDPMScheduler
#     num_train_timesteps: 100
#     beta_start: 0.0001
#     beta_end: 0.02
#     beta_schedule: squaredcos_cap_v2
#     variance_type: fixed_small # Yilun's paper uses fixed_small_log instead, but easy to cause Nan
#     clip_sample: True # required when predict_epsilon=False
#     prediction_type: epsilon # or sample

#   horizon: ${horizon}
#   n_action_steps: ${eval:'${n_action_steps}+${n_latency_steps}'}
#   n_obs_steps: ${n_obs_steps}
#   num_inference_steps: 100
#   obs_as_global_cond: ${obs_as_global_cond}
#   crop_shape: [76, 76]
#   # crop_shape: null
#   diffusion_step_embed_dim: 128
#   down_dims: [512, 1024, 2048]
#   kernel_size: 5
#   n_groups: 8
#   cond_predict_scale: True
#   obs_encoder_group_norm: True
#   eval_fixed_crop: True

  # scheduler.step params
  # predict_epsilon: True

ema:
  _target_: diffusion_policy.model.diffusion.ema_model.EMAModel
  update_after_step: 0
  inv_gamma: 1.0
  power: 0.75
  min_value: 0.0
  max_value: 0.9999

dataloader:
  batch_size: 64 #128 # (A6000) 128 # 64
  num_workers: 8 # 16 # (A6000) # 16
  shuffle: False #True
  pin_memory: False #True
  persistent_workers: False

val_dataloader:
  batch_size: 32
  num_workers: 4
  shuffle: False
  pin_memory: True
  persistent_workers: False

optimizer:
  _target_: torch.optim.AdamW
  lr: 1.0e-4
  betas: [0.95, 0.999]
  eps: 1.0e-8
  weight_decay: 1.0e-6

training:
  device: "cuda:0"
  seed: 42
  debug: False
  resume: True
  # optimization
  lr_scheduler: cosine
  lr_warmup_steps: 500
  num_epochs: 1400 #3050 (Original DP)
  gradient_accumulate_every: 1
  # EMA destroys performance when used with BatchNorm
  # replace BatchNorm with GroupNorm.
  use_ema: True
  # training loop control
  # in epochs
  rollout_every: 50
  checkpoint_every: 10
  val_every: 1
  no_validation: True
  sample_every: 1
  # steps per epoch
  max_train_steps: null
  max_val_steps: null
  # misc
  tqdm_interval_sec: 1.0

logging:
  project: diffusion_policy_debug
  resume: True
  mode: online
  name: ${now:%Y.%m.%d-%H.%M.%S}_${name}_${task_name}
  tags: ["${name}", "${task_name}", "${exp_name}"]
  id: null
  group: null

checkpoint:
  topk:
    monitor_key: train_loss #test_mean_score
    mode: min
    k: 50
    format_str: 'epoch={epoch:04d}-train_loss={train_loss:.3f}.ckpt'
  save_last_ckpt: True
  save_last_snapshot: False

multi_run:
  run_dir: data/outputs/${now:%Y.%m.%d}/${now:%H.%M.%S}_${name}_${task_name}
  wandb_name_base: ${now:%Y.%m.%d-%H.%M.%S}_${name}_${task_name}

hydra:
  job:
    override_dirname: ${name}
  run:
    dir: data/outputs/${now:%Y.%m.%d}/${now:%H.%M.%S}_${name}_${task_name}
  sweep:
    dir: data/outputs/${now:%Y.%m.%d}/${now:%H.%M.%S}_${name}_${task_name}
    subdir: ${hydra.job.num}
